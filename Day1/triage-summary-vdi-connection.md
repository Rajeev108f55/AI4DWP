# Triage Summary — Cannot Connect to VDI

## Summary
User cannot connect to their VDI (virtual desktop) today, receiving a "cannot connect" message; it worked on Friday.

## Impact
- **Who:** Single user reporting (to confirm — user name/ID not provided).
- **How many affected:** 1 (to confirm whether other remote/home users are experiencing the same VDI issue).
- **Business urgency:** To confirm — no role/criticality stated. Inability to access VDI likely blocks all work for a remote user, but urgency should be confirmed with the user/manager.

## Known Facts
- User cannot connect to "the VDI thing" today.
- Error message states "cannot connect."
- VDI access worked on Friday (last known working day).
- User is at home, connecting via Wi-Fi.

## Missing Information to Gather
- User's name, ID, and contact details (to confirm).
- Exact error message/code shown on screen (to confirm).
- Which VDI client/portal is being used (e.g. Citrix, Horizon, AVD) (to confirm).
- Time(s) the connection was attempted, and whether it fails every time or intermittently (to confirm).
- Whether other websites/internet services work normally on the same Wi-Fi (to confirm).
- Whether the user is using a DWP-issued device or personal device (to confirm).
- Whether the user has tried a wired connection, different Wi-Fi network, or rebooted the router/device (to confirm).
- Any VPN or additional authentication step required before VDI, and whether that step succeeds (to confirm).
- Whether MFA/token prompts were seen and completed successfully (to confirm).
- Whether the user changed their password recently or has any account lockout notifications (to confirm).

## Likely Category
Remote access / VDI connectivity issue — could relate to network (home Wi-Fi/ISP), VPN/authentication, or VDI service-side issue. Category to confirm once more detail is gathered.

## Suggested First Diagnostic Step
Ask the user to confirm whether general internet access (e.g. a public website) works on the same Wi-Fi, and check the DWP VDI/remote access service status dashboard for any reported outage before investigating the user's local device/network further.
