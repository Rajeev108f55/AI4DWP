Resolved. Cause: Overnight image update to POOL-FIN-01 (02:00, 2024-03-15) introduced an incompatible Intel GPU driver (igdumd64.dll v31.0.101.4146) that caused dwm.exe to crash on every user logon, producing a black screen — confirmed by App Event 1000 and DWM Event 9009 on affected session hosts. Action: All updated POOL-FIN-01 hosts placed into Drain mode; affected users force-logged off and re-brokered; session hosts rolled back to pre-update image (build-20240313) and verified clean via DWM Event 9011 before returning to production at 10:00. Preventive: Mandatory canary host validation gate to be added to the image release pipeline — new image must be deployed to one host, a test session confirmed clean (Event 9011, no Event 1000 for dwm.exe), before fleet rollout proceeds; OEM GPU drivers to be excluded or pinned in the image build; separate update waves per pool with minimum 24h gap to be enforced. User confirmed working.
